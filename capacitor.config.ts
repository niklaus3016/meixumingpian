import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.meixumingpian.app',
  appName: '美序名片',
  webDir: 'dist',
  android: {}
};

export default config;
